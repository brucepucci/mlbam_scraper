CREATE TABLE IF NOT EXISTS runner (
    atbat_id INT,
    gid VARCHAR(255),
    id INT,
    start VARCHAR(255),
    end VARCHAR(255),
    event VARCHAR(255),
    event_num INT,
    score VARCHAR(255)
);
